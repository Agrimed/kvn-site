services:
  frontend:
    image: kvn-site:latest
    container_name: kvn-site
    build:
      context: .
      dockerfile: Dockerfile
    restart: unless-stopped
    ports:
      - "127.0.0.1:8080:80"
