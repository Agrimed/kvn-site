services:
  frontend:
    image: kvn-site-dev:latest

    container_name: kvn-site-dev

    build:
      context: .
      dockerfile: Dockerfile.dev

    ports:
      - "5173:5173"

    volumes:
      - .:/app
      - /app/node_modules

    restart: unless-stopped
